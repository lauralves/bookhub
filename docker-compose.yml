services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: "bookhub"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "admin"
    ports:
      - "5432:5432"
    volumes:
      - ./docker/postgres/initdb.sql:/docker-entrypoint-initdb.d/initdb.sql
